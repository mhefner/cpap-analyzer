apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: cpap-analyzer
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/youruser/cpap-analyzer-deploy.git
    targetRevision: HEAD
    path: overlays/dev
  destination:
    server: https://kubernetes.default.svc
    namespace: default
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
