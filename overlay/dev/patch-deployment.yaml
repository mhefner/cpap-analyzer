apiVersion: apps/v1
kind: Deployment
metadata:
  name: cpap-analyzer
spec:
  template:
    spec:
      containers:
        - name: cpap-analyzer
          image: docker push mhefner1983/cpap-analyzer:dev  # tag it differently for dev
          imagePullPolicy: Always
